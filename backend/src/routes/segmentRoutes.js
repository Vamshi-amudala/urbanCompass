import { Router } from 'express';

// Placeholder for future segment-related endpoints (e.g., city data overlays)
const router = Router();

router.get('/', (req, res) => res.json({ ok: true }));

export default router;


